# Generated by the workflows in .github/workflows/ and published to the
# `cards` and `output` branches — never to main. Both publish steps use
# `git add -f`, so ignoring these here does not affect them.
profile-summary-card-output/
dist/
snake.svg
snake-dark.svg
